name: vercel_dart
description: A utility library for writing serverless functions in the Vercel Dart Runtime.
version: 1.0.0
repository: https://github.com/frencojobs/vercel-dart

publish_to: none # until the PR #24 is merged

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  aws_lambda_dart_runtime:
    git:
      url: https://github.com/frencojobs/aws-lambda-dart-runtime
      ref: dev
  json_annotation: ^4.1.0
  shelf: ^1.2.0

dev_dependencies:
  build_runner: ^2.1.2
  json_serializable: ^5.0.2
  lint: ^1.7.1
  test: ^1.17.12
